#include <bits/stdc++.h>
using namespace std;

typedef unsigned long long int ull;
typedef long long int ll;

ll t, k, p, n, ans;

int main(void)
{
  cin >> t;

  for (ll y = 1; y <= t; ++y)
  {
    ans = 0;
    cout << "Case #" << y << ": " << ans << '\n';
  }
}